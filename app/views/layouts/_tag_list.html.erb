<div class="side-bar-tag">
  <% tag_list.each do |tag|%>

    <div class="tag-btn">
      <%= link_to search_tag_post_path(tag) do %><% end %>
      <i>#</i><%= tag.tag_name %>
    </div>

  <% end %>
</div>

