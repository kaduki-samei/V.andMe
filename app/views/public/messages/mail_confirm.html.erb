<div class="theme">ご意見・お問い合せ 確認画面</div>
<p class="red">まだ送信は完了していません。</p>
<p class="mb-3">下記の内容で送信してよろしいですか？</p>

<div class="form-group">
  <p class="label">お名前</p>
  <div class="confirm-form"><%= @message.name %></div>
</div>

<div class="form-group">
  <p class="label">返信先メールアドレス</p>
  <div class="confirm-form"><%= @message.email %></div>
</div>

<div class="form-group">
  <p class="label">件名</p>
  <div class="confirm-form2"><%= @message.title %></div>
</div>

<div class="form-group mb-2">
  <p class="label">本文</p>
  <div class="confirm-form3"><%= safe_join(@message.body.split("\n"),tag(:br)) %></div>
</div>


<div class="d-flex flex-row">
  <%= form_with model: @message, url: back_messages_path, local: true do |f| %>
    <%= f.hidden_field :name %>
    <%= f.hidden_field :email %>
    <%= f.hidden_field :title %>
    <%= f.hidden_field :body %>
    <%= f.submit "戻る", class: "btn blue-btn mr-2" %>
  <% end %>
  <%= form_with model: @message, url: messages_path, local: true do |f| %>
    <%= f.hidden_field :name %>
    <%= f.hidden_field :email %>
    <%= f.hidden_field :title %>
    <%= f.hidden_field :body %>
    <%= f.submit "送信", class: "btn orange-btn" %>
  <% end %>
</div>