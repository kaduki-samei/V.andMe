<h2>新規登録</h2>

<p><%= link_to "こちら", about_path %>の注意事項をご確認の上、会員登録を行ってください。</p>

<%= form_with model: @user, url: user_registration_path do |f| %>
  <div class="field">
    <%= f.label :ユーザー名 %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :"パスワード(半角英数８文字以上)" %><br />
    <%= f.password_field :password, autocomplete: "半角英数８文字以上" %>
  </div>
  <div class="field">
    <%= f.label :"パスワード(確認)" %><br />
    <%= f.password_field :password_confirmation, autocomplete: "再入力" %>
  </div>
  <div class="actions mt-3">
    <%= f.submit "登録", class: "blue-btn" %>
  </div>
<% end %>

<p class="mt-4">すでに登録済の方は<%= link_to "こちら", new_user_session_path  %>からログインしてください。</p>