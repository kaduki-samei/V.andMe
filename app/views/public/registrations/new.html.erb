<h2>新規登録</h2>

<p><%= link_to "こちら", about_path %>の注意事項をご確認の上、会員登録を行ってください。</p>

<%= form_with model: @user, url: user_registration_path do |f| %>
  <div class="field">
    <p class="label">ユーザー名</p>
    <%= f.text_field :name %>
    <% if resource.errors.include?(:name) %>
      <% resource.errors.full_messages_for(:name).each do |error_message| %>
        <p class="error"><%= error_message %></p>
      <% end %>
    <% end %>
  </div>
  <div class="field">
    <p class="label">パスワード(半角英数８文字以上)</p>
    <%= f.text_field :password, autocomplete: "半角英数８文字以上" %>
    <% if resource.errors.include?(:password) %>
      <% resource.errors.full_messages_for(:password).each do |error_message| %>
        <p class="error"><%= error_message %></p>
      <% end %>
    <% end %>
  </div>
  <div class="field">
    <p class="label">パスワード(確認)</p>
    <%= f.text_field :password_confirmation, autocomplete: "再入力" %>
  </div>
  <div class="actions mt-3">
    <%= f.submit "登録", class: "btn blue-btn" %>
  </div>
<% end %>

<p class="mt-4">すでに登録済の方は<%= link_to "こちら", new_user_session_path %>からログインしてください。</p>
