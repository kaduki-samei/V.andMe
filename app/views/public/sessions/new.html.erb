<h2>ログイン</h2>

<%= form_with model: @user, url: user_session_path do |f| %>
  <div class="field">
    <%= f.label :ユーザー名 %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :パスワード %><br />
    <%= f.password_field :password, autocomplete: "current-password" %>
  </div>

  <div class="actions mt-3">
    <%= f.submit "ログイン", class: "blue-btn" %>
  </div>
<% end %>

<p class="mt-4">ご登録がまだの方は<%= link_to "こちら", new_user_registration_path %>より会員登録をしてください。</p>